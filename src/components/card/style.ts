import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  card: {
    boxShadow: "0px 0px 5px 0px rgba(112,112,112,0.71)",
    padding: 20,
    borderRadius: 6,
    marginBottom: 10,
  },
});
